---
title: 'Status'
api: 'GET /public/workflows/runs/{run_id}'
---

```bash
curl https://catalogapi.rastro.ai/api/public/workflows/runs/{run_id} \
  -H "Authorization: Bearer rastro_pk_..."
```

**Response (running):**

```json
{
  "status": "running",
  "progress": 0.5
}
```

**Response (complete):**

```json
{
  "status": "completed",
  "results": {
    "data": [
      {"sku": "A1", "title": "Product A", "enriched": "..."}
    ]
  }
}
```
